/**
 * @file datapos-connector-data-file-store-emulator/rollup.config-umd.js
 * @description Rollup configuration file for generating UMD module bundle of the data file store emulator connector.
 * @license ISC Licensed under the ISC license, Version 2.0. See the LICENSE.md file for details.
 * @author Jonathan Terrell <terrell.jm@gmail.com>
 * @copyright 2023 Jonathan Terrell
 */

// TODO: Upgrade syntax for JSON file imports. See: https://rollupjs.org/guide/en/#importing-packagejson.
// The latest syntax (line below) triggers a VSCode ESlint error, so temporarily using older 'require' syntax.
// import config from './src/config.json' assert { type: 'json' };
import { createRequire } from 'node:module';
const require = createRequire(import.meta.url);
const config = require('./src/config.json');

import camelcase from 'lodash.camelcase';
import commonjs from '@rollup/plugin-commonjs';
import nodeResolve from '@rollup/plugin-node-resolve';
import json from '@rollup/plugin-json';
import typescript from 'rollup-plugin-typescript2';
import { terser } from 'rollup-plugin-terser';

export default [
    {
        // TODO: context: 'this', // Required to suppress '`this` has been rewritten to `undefined`' warning generated by 'p-queue'.
        input: 'src/index.ts',
        output: [
            {
                exports: 'auto',
                file: `./dist/${config.id}-umd.js`,
                format: 'umd',
                name: camelcase(config.id)
            }
        ],
        // TODO:
        // onwarn: (message, warn) => {
        //     if (message.code === 'THIS_IS_UNDEFINED') return;
        //     warn(message);
        // },
        plugins: [nodeResolve({ browser: true }), commonjs(), json(), typescript(), terser({ output: { comments: false } })]
    }
];
